define(['goo/entities/Bus'],
/** @lends */
function (Bus) {

	'use strict';

	/**
	 * @name SystemBus
	 * @class
	 * SystemBus is a global instance of the {@link Bus} class.
	 */

	return new Bus();
});
