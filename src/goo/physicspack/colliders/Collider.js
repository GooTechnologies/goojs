define([],
/** @lends */
function () {
	'use strict';

	/**
	 * @class
	 * @param {object} [settings]
	 */
	function Collider(settings) {
		settings = settings || {};
	}
	Collider.constructor = Collider;

	return Collider;
});
