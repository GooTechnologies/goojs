Tools
=====

Holds various tools for building the engine, the documentation, tern definitions, detect require cycles, strip/process `#ifdef` and so on.
 
Every tool/utility comes with its own documentation.